{
  "name": "erd-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build && cd ../backend && npm install && npx tsc",
    "start": "node backend/dist/index.js"
  },
  "dependencies": {}
}